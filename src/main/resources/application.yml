spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: pushpa20052002kumari@gmail.com
    password: eawf owun xxcs gemq
    tls: true
    properties.mail.smtp:
      auth: true
      starttls.enable: true
#      thymeleaf:
#          prefix: classpath:/templates/
#          suffix: .html

  datasource:
    url: jdbc:postgresql://localhost:5432/hospital
    username: postgres
    password: pushpa@9798
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081

